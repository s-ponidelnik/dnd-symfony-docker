<form method="post" action="{{ path('spell_school_delete', {'id': spell_school.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spell_school.id) }}">
    <button class="btn">Delete</button>
</form>
